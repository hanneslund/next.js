import React from 'react'
import { Inter } from '@next/font/google'

const inter1 = Inter({
  variant: '900',
  display: 'swap',
})

const inter2 = Inter({
  variant: '900',
  display: 'swap',
})

console.log(inter1, inter2)
